//ES6 Modules

// export const add = (x, y) => x + y;
// export const diff = (x, y) => Math.abs(x - y);

//common JS

const add = (x, y) => x + y;
const diff = (x, y) => Math.abs(x - y);

module.exports = {add, diff}